# CloudBase 环境交接说明

> **交接方**：Kevin ｜ **接手方**：waixr016、fodenspider ｜ **建立日期**：2026-09-19
>
> **本文是什么**：CloudBase（腾讯云开发）环境的**操作手册** —— 回答"这个环境是什么、怎么连进来、有什么坑"。
> 实测数据与探针细节见《云开发实测记录》，本文不重复抄。
>
> ⚠️ **本文不含任何密钥。** envId 与 API Key 由 Kevin 通过私聊单独发送，**不要写进仓库、不要贴进文档、不要截图**。
>
> **状态标注**：`【已实测】` 亲自跑通 ｜ `【已核实】` 官方文档或 API 返回值 ｜ `【待验证】` 未测。

---

## 0. 先读这一段：三条硬约束

**在写任何代码之前先确认这三条，否则会返工。**

| # | 约束 | 影响 |
|---|---|---|
| 1 | **只有 PostgreSQL，没有文档型数据库** | `RuntimeBackends = {postgresql: true, nosql: false, mysql: false}` `【已实测】`；且**创建环境时类型二选一、之后不可更改** `【已实测】`。→ 方案里凡是写"CloudBase 文档数据库 / 集合（collection）/ `app.database()`"的地方，**必须改写成 PG 表 + SQL**，否则代码落不了地 |
| 2 | **不能用 `pg` 协议直连** | 实例是共享集群（`TenantType: SHARED`），无外网 IPv4，控制台原文提示"当前共享集群暂不提供相关能力"。→ 数据访问**只有 HTTP / PostgREST 这一条路**，**不需要、也不能用连接池** |
| 3 | **免费账号只有 1 个环境** | 沿用现有环境即可；**如果要新建环境，现有环境连同其中的数据会被替换** `【已实测】`。动手前先说一声 |

> **好消息（2026-09-19 更新）**：《AI 实施任务书》v1.0 §1 已把架构定为
> **Next.js → Vercel Functions（hkg1）→ Repository 适配层 → HTTPS/PostgREST → CloudBase PostgreSQL 17**，
> 并**明令禁止**改为「CloudBase 静态托管主体 / **CloudBase 文档数据库** / Supabase 或 Neon」。
> 也就是说上面三条硬约束与任务书**完全一致，不需要再改设计**。
> 本环境是这套架构的**已就绪靶子**，直接建表即可。

---

## 1. 环境事实 `【已实测】`

| 项 | 值 |
|---|---|
| 环境 ID | `kevin-cloud-server-d8c078fa24b09` |
| 别名 | `kevin-cloud-server` |
| 归属 | Kevin 个人腾讯云账号，**不是团队账号** |
| 套餐 | 体验版（`baas_trial`，免费），**到期 2027-03-18 23:59:59**，`IsAutoRenew: false` |
| 续期 | 免费环境单次可续 6 个月，**需在到期前 1 个月内手动续**；另有官方"社交分享 0 元续期"活动 |
| 超限行为 | `EnableOverrun: false` → 资源点用完即停，**不会自动扣费** |
| 地域 | `ap-shanghai`（上海） |
| 后端模式 | `RuntimeMode: postgresql` |
| 数据库实例 | `pgdb-efkzsjnh`，**PostgreSQL 17.11**，共享实例 |
| 环境 QPS 配额 | 500 |
| 日志保留 | CLS 日志集 **7 天** |
| 静态托管域名 | `kevin-cloud-server-d8c078fa24b09-1437126399.tcloudbaseapp.com`（服务 online、路由已开）|
| 对象存储桶 | `6b65-kevin-cloud-server-d8c078fa24b09-1437126399`（**legacy NoSQL 桶**，见 §6）|

> ⚠️ 静态托管的**默认域名在生产环境不可直接使用**：官方称默认域名仅适用于开发测试，且自 2025-10-09 后创建的环境长期带"访问提示中间页"；正式对外必须用自定义域名，而自定义域名**必须 ICP 备案**。详见《云开发实测记录》§2.5。

---

## 2. 怎么连进来

### 2.1 方式一：客户端连接器（推荐，日常开发用这个）

**用 API Key 登录，不是扫码。** 凭据（envId + API Key）由 Kevin 私聊提供。

登录成功后连接器会显示：

- `auth_mode: api_key`
- `credential_scope: single_env`
- `current_env_id: kevin-cloud-server-d8c078fa24b09`

> **`single_env` 是权限边界，不是故障。** 环境级凭据只能看到这一个环境，**看不到 Kevin 账号下的其他环境或其他地域**，这是设计如此，不要去排查"为什么环境列表是空的"。

**这把 Key 能做什么** `【已实测】`：

| 能力 | 状态 |
|---|---|
| 查询环境详情 / 资源 | ✅ |
| 数据库：读（SQL、对象列表、表结构） | ✅ |
| 数据库：写（建表、DML、RLS） | ✅（建表走 migration，见 §4） |
| 云函数：列出 / 查看 | ✅ |
| 静态托管：状态 / 文件查询 | ✅ |
| 部署云函数、写托管文件 | `【待验证】` —— 权限上应可用（见 §5 的 service_role），但本次未实跑 |

### 2.2 方式二：HTTP / PostgREST（任何语言、任何工具都能用）

不依赖任何客户端，**这是最通用的接入方式**，也是《AI 实施任务书》选定的数据访问路径。
变量名与任务书 §4.1 保持一致：

```bash
# 环境变量（值由 Kevin 私发，填进本地 .env.local 与 Vercel Dashboard）
# CLOUDBASE_PG_REST_BASE_URL = https://kevin-cloud-server-d8c078fa24b09.api.tcloudbasegateway.com
# CLOUDBASE_PG_API_KEY       = <由 Kevin 私发，勿写入任何仓库文件>

# 读
curl -s -H "Authorization: Bearer $CLOUDBASE_PG_API_KEY" \
  "$CLOUDBASE_PG_REST_BASE_URL/v1/rdb/rest/<表名>?select=*&limit=5"

# 写（PostgREST 惯用法）
curl -s -X POST -H "Authorization: Bearer $CLOUDBASE_PG_API_KEY" \
  -H "content-type: application/json" -H "Prefer: return=representation" \
  -d '[{"字段":"值"}]' \
  "$CLOUDBASE_PG_REST_BASE_URL/v1/rdb/rest/<表名>"

# 调 RPC（对应任务书 §6.2 的那批函数）
curl -s -X POST -H "Authorization: Bearer $CLOUDBASE_PG_API_KEY" \
  -H "content-type: application/json" -d '{"参数":"值"}' \
  "$CLOUDBASE_PG_REST_BASE_URL/v1/rdb/rest/rpc/<函数名>"
```

- 网关：`https://<envId>.api.tcloudbasegateway.com`
- 表操作路径：`/v1/rdb/rest/<表名>`
- 条件过滤：`?列=eq.值`；更新用 `PATCH`，删除用 `DELETE`
- 计数：加请求头 `Prefer: count=exact`，从响应头 `content-range` 读

**认证是否通过的判据**：带 Key 但表不存在时返回 `DATABASE_PGRST205: Could not find the table 'public.<表名>' in the schema cache` —— **这条错误恰恰证明认证通过、路由正确**。

⚠️ **公网网关没有 SQL 入口**（`/v1/rdb/sql` 返回 404）。但**建表、改 schema、跑任意 SQL 都是能做到的** —— 走客户端连接器 / 控制台 SQL 编辑器 / CLI 即可。日常开发不受影响。

> `【待验证】` 表的 CRUD 已实测全通（见《云开发实测记录》§2.2）。
> **`/v1/rdb/rest/rpc/<函数名>` 这条路径本次未测**（库里目前还没有 RPC 函数）。
> 任务书 §6.2 的那批 RPC 建好后，**请第一时间用上面的 curl 打通一次**，再往下写业务代码。

### 2.3 方式三：CloudBase CLI（`tcb`）

可用但配置繁琐，作为备选。已知坑见 §7 第 1、2、3 条（登录前要先处理 `cloudbaserc.json`）。

### 2.4 对《AI 实施任务书》§3.3 确认清单的答复

任务书列了 5 个需要人工确认的问题，这里直接给出答案（**均基于 2026-09-19 实测**）：

| 任务书的问题 | 答复 |
|---|---|
| CloudBase PostgreSQL 环境已可用 | **是** —— `kevin-cloud-server-d8c078fa24b09`，PostgreSQL 17.11，`public` schema 干净、无业务表 |
| PostgREST Base URL 已取得 | **是** —— `https://kevin-cloud-server-d8c078fa24b09.api.tcloudbasegateway.com` |
| 服务端 API Key 已创建 | **是** —— 权限为 `service_role`（管理员级），由 Kevin 私发，**不进仓库、不进文档** |
| 可以执行迁移 SQL | **是** —— 控制台 SQL 编辑器 / CloudBase CLI / 客户端连接器，三条路都能跑 DDL |
| 是否有独立测试环境 | **否** —— 免费账号只有 1 个环境且类型不可改。**按任务书 §4.3 的降级要求执行** |

**"没有独立测试环境"怎么落地**（三选一，推荐第 1 个）：

1. **独立 schema**（推荐）—— 测试数据建在 `public_test`，正式在 `public`。
   PostgREST 默认只暴露 `public`，`public_test` 不会污染正式接口，隔离干净且零成本。
2. **`account_type = 'demo'` 标记** + Repository 层过滤（任务书 §4.3 已提到这条）。
3. 新建一个 CloudBase 环境专供测试 —— **不推荐**，见 §0 第 3 条。

---

## 3. 数据库现状 `【已实测】`（2026-09-19 复核）

| 项 | 值 |
|---|---|
| 数据库名 | `pgdb-efkzsjnh` |
| 版本 | PostgreSQL 17.11 |
| 当前角色 | `cloudbase_postgres_pgdb_efkzsjnh` |
| 已装扩展 | `plpgsql 1.0`、**`vector 0.8.2`（pgvector）** |
| 可装未装 | `postgis 3.5.0`、`pg_trgm 1.6`、`uuid-ossp 1.1`、`hstore 1.8` |
| 迁移历史 | 2 条：`20260918232209_probe_init`、`20260918234741_vector_probe` |

**对象清点（共 29 个）**：

- `public` —— **业务区。目前只有 2 张探针残留表**（`cb_probe` 0 行、`cb_vec_probe` 3 行），**没有任何业务表**。
- `auth`（19 张）—— 平台自带认证体系（`users` / `token` / `identity` / `mfa_factors` …）。**只读，不要改**。
- `storage`（5 张）—— 平台自带存储元数据。**只读，不要改**。
- `cloudbase_migrations`（3 张）—— 迁移历史表。**由工具维护，不要手改**。

> **结论：数据库是干净的，业务表为空，可以放心按新设计建表。** 唯一遗留物是上面两张探针表，清理 SQL 见 §8。

---

## 4. schema 变更怎么做（重要）

**建表 / 改表必须走 migration，不要用裸 SQL。** 任务书 §6.3 也明确了这点：
**迁移不能由 Vercel 运行时执行**，必须人工在控制台 / CLI / 连接器里跑。

### 4.1 ⚠️ 两套文件命名约定，先对齐再动手

| | 任务书 §6.1 的约定 | 连接器 `applyMigration` 的约定 |
|---|---|---|
| 目录 | `scripts/migrations/` | `cloudbase/migrations/` |
| 文件名 | `001_extensions.sql`、`002_core_tables.sql` … | `<14位时间戳>_<name>.sql`，如 `20260920103000_init_core_tables.sql` |
| 名称约束 | 无 | 小写字母开头，**只能含小写字母和下划线，不能有数字** |
| 版本号 | 序号 | **14 位时间戳** `YYYYMMDDHHMMSS`，自己给，不要自动生成 |

**这两套不是二选一，而是"内容"与"投递"的关系** —— 建议：

- **人写的源文件按任务书 §6.1 放在 `scripts/migrations/`**（序号清晰、便于按阶段阅读和 review）；
- **实际执行时走连接器 `applyMigration`**，它会在 `cloudbase/migrations/` 落一份 `版本号_名称` 的同内容 SQL，
  并把它写进远端 `cloudbase_migrations.schema_migrations` 历史。
- **两个目录都要提交进仓库**，`cloudbase/migrations/` 是"线上到底跑过哪些迁移"的唯一权威记录。

> ⚠️ 不要绕开 `applyMigration` 直接用 `execute` 跑 DDL（默认会被拒绝）。绕过去的话，远端迁移历史会和仓库文件脱节，
> 后面想用 `fetchMigration` 对齐就对不上了。

### 4.2 其他要点

- `applyMigration` **不是增量**：每次要传**完整** SQL
- 可选传入回滚 SQL（`rollbackSql`）
- 执行前先 `planMigration` 预览，确认无误再 `apply` 并带 `confirm=true`
- 失败时先 `listMigrations` 确认版本有没有落库，**不要立刻用同一个版本号重推**

```
scripts/migrations/          # 人写源文件（任务书 §6.1 的编号体系）
├── 001_extensions.sql
├── 002_core_tables.sql
└── ...

cloudbase/migrations/        # 连接器实际投递的记录（14 位时间戳）
└── 20260920103000_init_core_tables.sql
```

> 当前仓库里这两个目录**都还不存在**，第一次投递时工具会自动创建 `cloudbase/migrations/`。

---

## 5. 权限模型：PG 表走 RLS

- PG 表的权限控制是 **Row-Level Security**：
  ```sql
  ALTER TABLE public.xxx ENABLE ROW LEVEL SECURITY;
  CREATE POLICY "..." ON public.xxx FOR SELECT USING (...);
  ```
- ⚠️ **`managePermissions(resourceType="noSqlDatabase")` 对 PG 表完全无效** —— 那套规则只管 NoSQL 集合。
- 可用角色：`cloudbase_postgres`（默认）、`anon`、`authenticated`、`service_role`
- **API Key 的权限等级**：解码 JWT 可见 `role: service_role` + `is_system_admin: true` `【已实测】`
  → **这是管理员级权限**，等于环境的完全控制权。因此：
  1. **绝不能出现在前端代码、浏览器请求、公开仓库里**
  2. 前后端分离时，API Key 只放服务端环境变量（本项目约定用 `SERVER_` 前缀）
  3. 浏览器端要访问数据，走匿名/登录态 + RLS 策略，而不是把 Key 发下去

---

## 6. 存储：PG 模式和你想的不一样 `【已核实】`

平台明确提示：PG 模式下浏览器上传必须用 **pgstore**：

```js
app.storage.from().upload('<bucket>/<key>', file)   // bucket 需先显式创建
```

- ⚠️ **`EnvInfo.Storages[]` 里列出的桶是 legacy NoSQL 桶，不是有效的 pgstore 目标。**
  直接拿它做浏览器上传会得到 `STORAGE_BUCKET_NOT_FOUND`，或请求打到 `PUT https://undefined/`。
- ⚠️ 本环境**没有可用的 NoSQL 实例**，因此**不要假设 `app.database()` 能用**。
- 云存储存在 ≠ 数据库是文档型。这两件事是分开的。

---

## 7. 踩坑清单（只列交接后最可能踩到的）

完整 11 条见《云开发实测记录》§6。这里挑最相关的：

| # | 现象 | 对策 |
|---|---|---|
| 1 | 连接器显示"没有可用环境" | 环境级凭据是 `single_env`，**这是正常的**；确认登录方式是否为 API Key |
| 2 | `tcb login` 报「配置文件必须包含环境 Id」 | CLI 强制校验 cwd 下的 `cloudbaserc.json`；登录前先把它改名（如 `.tpl`） |
| 3 | CLI 设备码授权页只显示「暂无可用环境」 | **必须"先建环境 → 再授权"**；设备码有效期仅几分钟，要当场点 |
| 4 | `createFunction` 报 `additionalProperties` | 管控面接口不接受 `memorySize` 字段（免费版内存本就固定 256MB） |
| 5 | `createFunction type:"HTTP"` 报 `ResourceNotFound.BootstrapFile` | HTTP 型云函数**必须自带 `scf_bootstrap`**（自起 HTTP 服务监听 9000）；传统形态用 `type:"Event"` |
| 6 | `createFunction` 报「不支持修改…(HOME)」 | **平台禁止覆盖 `HOME`** → 改为在函数代码里 `process.env.HOME='/tmp'` 并注入子进程 |
| 7 | `updateFunctionCode` 报 `FUNCTION_UPDATING` | 上一次更新还在跑；**等 10 秒且状态 Active 再重试，不要连打** |
| 8 | 静态托管默认域名访问异常 | 见 §1 的警告：默认域名仅测试用，正式要自定义域名 + ICP 备案 |

---

## 8. 遗留物与清理

**云函数**：`diagnose`（Event 型 / Nodejs18.15 / Active）—— 诊断探针，可留着复用，也可删。
（`fatprobe` / `fatprobe2` / `sdkprobe` 已删除，`sdkprobe` 因持有 API Key 环境变量已按承诺测完即删。）

**数据表**：两张探针残留表，确认不需要后执行：

```sql
drop table if exists public.cb_probe;
drop table if exists public.cb_vec_probe;
```

**扩展**：`vector`（pgvector）**建议保留** —— 向量检索要用它，0.8.2 已实测可用（L2 与余弦距离排序均正确）。

---

## 9. 接手前需要确认的几件事

**已经解决、不用再讨论的：**

- ✅ **技术路线一致** —— 任务书 §1 明令禁止「CloudBase 文档数据库 / 静态托管主体 / Supabase / Neon」，
  与本文 §0 的三条硬约束完全吻合，**不需要改设计**。
- ✅ **数据访问层** —— 任务书 §7.1 已指定 `server/repositories/cloudbase-postgrest.ts` + 接口抽象，
  符合"外部依赖走适配器层"的项目约定（`pg` 直连不可用，只能用 HTTP/PostgREST，所以这层必须有）。

**还需要人拍板的：**

1. **建表分工**：谁负责第一次 migration？注意 §4.1 的两套目录约定，避免两人各自建表。
2. **测试数据隔离方式**：独立 schema / `demo` 标记 / 不隔离？（见 §2.4 的三个选项）
3. **API Key 纪律**：只放服务端环境变量；`.env.local` 必须 gitignore；
   **不写进源码、日志、Markdown 或聊天记录**（任务书 §2 第 9 条、§4.2 的原始要求）。
4. **评审风险**：环境在 Kevin 个人账号下、免费版单环境，**重建即丢数据**；
   另外 `*.vercel.app` 在中国大陆的可达性**仍未实测**（上线首日必须用手机 4G 试）。

---

## 相关文档

| 文档 | 作用 |
|---|---|
| 《云开发实测记录》 | 探针实测数据、完整坑表（11 条）、体积与计费测算、未验证项清单 |
| 《总控文档》 | 团队唯一执行入口（开发步骤、基准文档清单、agent 使用规则） |
| ~~《项目决策记录》~~ | **已于 2026-09-19 删除**（记录的是已被推翻的旧技术栈）；历史版本见 git 记录 |
