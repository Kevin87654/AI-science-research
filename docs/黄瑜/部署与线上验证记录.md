# 部署与线上验证记录

> **团队记录文档** —— 记录**线上环境是什么样、验证到什么程度、哪些还没验**。
> 与基准文档的区别：本文**会随部署变化而更新**，不是不可变的事实基准。
>
> **本文回答**：线上在哪、怎么部署的、验证跑出了什么结果、中国大陆能不能打开、还没验什么。
> **本文不回答**：数据库怎么写（看《数据库接入探针记录》）、环境怎么接（看《数据库交接说明》）、产品要做什么（看 PRD）。
>
> **状态标注**：`【已实测】` 亲自跑出 ｜ `【已核实】` 官方文档或返回值 ｜ `【待验证】` 未测
>
> ⚠️ **本文不含任何密钥。** 两个 `SERVER_CLOUDBASE_*` 变量的值只在 Kevin 本地的 `.env.local` 与 Vercel 项目的环境变量里，**不要写进仓库、不要贴进文档、不要截图**。

---

## 0. 一句话结论

**线上已可用，数据库连通，隔离验收 44/44 全绿。** 唯一还挡着的是**大陆访问**——`*.vercel.app` 默认域名在大陆打不开，换成自定义域名即可，见 §5。

---

## 1. 线上地址与部署形态

| 项 | 值 |
| --- | --- |
| 线上地址 | `https://ai-science-research.vercel.app` |
| 平台 | Vercel（**Hobby / 免费版**） |
| 账号归属 | Kevin 的个人账号（`kevin87654's projects`） |
| 函数区域 | **`hkg1`（中国香港）** |
| 部署来源 | GitHub `Kevin87654/AI-science-research` 的 `main` 分支，**Git 集成自动部署** |
| 首次部署 | 2026-09-20 23:23 |
| 运行时 | Node.js **24.x**（由 `package.json` 的 `engines` 指定） |
| 包管理器 | pnpm 11.19.0（由 `package.json` 的 `packageManager` 指定） |

**区域证据**【已实测】：`/api/ping` 的响应头带 `X-Vercel-Id: sfo1::hkg1::…`，其中冒号中间那段 `hkg1` 即函数实际执行区域。

⚠️ **Hobby 计划只能配 1 个区域**，多配会在构建前直接失败。区域来自仓库根目录 `vercel.json` 的 `regions` 字段；控制台改过之后需要重新部署才生效。

### 谁能部署这个项目

Vercel 的官方规则（`【已核实】`，见官方知识库 *How to fix "unable to find your GitHub repository"*）：

> 个人账号持有的仓库，**导入者必须是仓库 Owner**；**协作者（Collaborator）不能导入，也不能连接**。

- 这**与仓库是否为 Public 无关**。Hobby 计划的设计是"只部署自己的个人仓库"。
- 因此**队友不能用自己的 Vercel 账号导入本仓库**。要变更部署，找 Kevin。
- ⚠️ 顺带的坑：**Hobby 计划不支持添加团队成员**（团队协作是 Pro 功能），所以"把队友加为 Vercel 成员"这条路也不通。
- ⚠️ 再顺带：**把仓库转进 GitHub Organization 并不能解决**——Hobby 项目不能连接组织持有的仓库，转了要连就得升级 Pro。

> 结论：**部署这件事的钥匙在 Kevin 一个人手上**，这是平台限制，不是流程设计。评审期内如果需要紧急改线上，要能联系到 Kevin。

---

## 2. 环境变量

线上**只需要两个**，且**名字必须带 `SERVER_` 前缀**：

| 变量名 | 必需 | 说明 |
| --- | --- | --- |
| `SERVER_CLOUDBASE_PG_REST_BASE_URL` | ✅ | 网关地址 |
| `SERVER_CLOUDBASE_PG_API_KEY` | ✅ | 管理员级密钥，**建议开启 Vercel 的 "hide value after saving"** |
| `SERVER_CODEBUDDY_API_KEY` | 暂不需要 | `.env.example` 里的占位。当前 `src/` 中没有任何代码引用它，AI 层未接。真正接 AI 时再补，补完只需重新部署一次 |

- Environment 至少勾 **Production + Preview**。
- 🚨 **变量名写错不会报错。** 应用找不到这两个变量会**静默退回内存实现**——页面照常打开，但数据每个请求一份、重启即清空。唯一露馅的地方是 `/api/ping` 会显示 `"database": "not_connected"`。**部署完必须用 §4 的第 1 步验证一次。**
- ⚠️ Vercel 的配置页会显示 "Environment Variables **N Detected**"——那是从仓库的 `.env.example` 读到的**变量名，值为空**。**不要直接点 Deploy**，必须展开把真值填进去。
- ⚠️ 粘贴值时留意别带多余的空格或换行（API Key 是 932 字符的长串，从别处复制容易多带换行）。

---

## 3. 部署前的本地基线【已实测】

在 `main` = `4beeeef` 上跑出的结果，作为线上结果的对照：

| 项目 | 结果 |
| --- | --- |
| `next build` | 通过（Turbopack，3 个静态路由 + 3 个动态路由） |
| `scripts/verify/database-layer.mjs`（对真实数据库直测） | **26 通过 / 0 未通过** |
| `scripts/verify/identity-isolation.mjs`（本地 `next start`） | **44 通过 / 0 未通过** |

---

## 4. 线上验证结果【已实测】

验证时间：2026-09-20 23:25（部署完成后几分钟内）。

### 4.1 自检接口

```
GET https://ai-science-research.vercel.app/api/ping
→ 200
{"ok":true,"data":{"application":"ready","integrations":{"database":"connected","ai":"not_connected"}}}
```

`"database": "connected"` 表示**两个环境变量都已生效、应用已切到真实数据库**（而不是内存实现）。
`"ai": "not_connected"` 是预期值——AI 层尚未接入。

延迟：连续 3 次测量 0.91–0.99 秒（含测试机的代理绕行开销，不代表终端用户真实体感）。

### 4.2 匿名身份与数据隔离验收（44 项）

```
node --env-file=.env.local scripts/verify/identity-isolation.mjs https://ai-science-research.vercel.app

结果：44 通过 / 0 未通过
```

**这是"两名匿名用户数据互相不可见"的线上证据**，覆盖 9 组用例：

| # | 用例组 | 结论 |
| --- | --- | --- |
| 1–2 | 匿名会话建立与身份确认（真实 Cookie 往返） | 通过 |
| 3 | 会话令牌不落库明文（只存 SHA-256） | 通过 |
| 4–5 | 两名匿名用户各自读写自己的进度 | 通过 |
| 6 | ⭐ **伪造他人 `userId` 写入 → 403 + `FORBIDDEN`，且不落库** | 通过 |
| 7 | 入参校验（无会话 401 / 非法状态 400 / 畸形 JSON 400 / 超长 400 / 缺字段 400） | 通过 |
| 8 | 回归：`/api/ping` 仍正常 | 通过 |
| 9 | 清理本轮创建的会话与进度（外键级联） | 通过 |

> 脚本会**读写真实数据库**，用 `--env-file=.env.local` 提供清理所需的凭据，只删除本轮 Cookie 对应的会话，跑完自动清干净。

### 4.3 首页

`GET /` 返回 200，Next.js 正常渲染（`lang="zh-CN"`）。当前是工程脚手架的占位首页，**产品页面尚未迁入**——这不影响本轮的部署与隔离结论。

---

## 5. 中国大陆可达性【已实测】

### 5.0 结论

**`*.vercel.app` 默认域名在中国大陆打不开；但 Vercel 本身在大陆是可达的，换成自定义域名即可。**

→ 这不是"要不要换平台"的问题，而是"要不要买一个域名"的问题。见 §5.3。

### 5.1 默认域名确实不可用

| 测试 | 结果 |
| --- | --- |
| 系统 DNS 解析 `ai-science-research.vercel.app` | 返回 `128.242.240.20` —— **不在 Vercel 的 IP 段内**（Vercel 用 `76.76.21.0/24` 与 `66.33.60.0/24`）→ 典型的 **DNS 污染** |
| 对该地址发起 TLS 连接 | 握手阶段被重置（`Recv failure: Connection was reset`） |
| 人工指定干净 IP、但仍用 `*.vercel.app` 作 SNI | 同样被重置 → **不只是 DNS 污染，这个域名本身也被针对** |
| 另一例 `swr.vercel.app` 解析 | `199.59.149.235`（同样是污染 IP），连接超时 |
| **中国电信校园网 / 中国电信移动数据** | **打不开**，浏览器提示"响应时间太长"【Kevin 真机实测】 |
| 挂境外节点（美国） | 可以打开【Kevin 真机实测】 |

⚠️ **注意区分两个域名**：被针对的是**部署出来的网站域名**（`*.vercel.app`）；**控制台域名 `vercel.com` 没有被污染**，大陆直连 200、仅 0.20 秒。两者经常被混为一谈。

### 5.2 对照实验：Vercel 的服务器在大陆并没有被封

用**已知由 Vercel 托管、但绑定了自定义域名**的站点做对照，全程**直连、不走代理**：

| 站点 | 解析到的 IP | 结果 |
| --- | --- | --- |
| `react.dev` | `76.76.21.61` ← **Vercel 原生 IP 段** | **200，1.45 秒** |
| `tRPC.io` | `76.76.21.21` ← **Vercel 原生 IP 段** | **200，1.92 秒** |
| `astro.build` | `104.18.0.156`（Cloudflare 前置） | 200，0.65 秒 |
| `zod.dev` | `172.67.180.179`（Cloudflare 前置） | 200，1.72 秒 |
| `vitest.dev` | `52.74.6.109` | 200，1.15 秒 |
| **`swr.vercel.app`**（默认域名） | `199.59.149.235`（污染 IP） | **超时失败** |

→ **同一家平台、同一批 IP 段：自定义域名能开，默认域名打不开。被针对的是 `*.vercel.app` 这个域名，不是 Vercel 的服务器。**

### 5.3 建议：买一个域名，只改这一处

**这是解决大陆访问的最短路径。**

- **不需要备案** —— 备案只针对"使用中国大陆境内服务器"的场景；域名解析到境外主机不需要备案。
- 后缀选便宜的即可（`.top` / `.xyz` / `.icu` 一类，约 ¥10–30/年）。
- DNS 解析：CNAME 指向 **`cname-china.vercel-dns.com`**（Vercel 面向中国大陆的专用入口，比默认的 `cname.vercel-dns.com` 多一个 `china`）。
  ⚠️ 如果 Vercel 的域名卡片报 `Invalid Configuration`，**先用它提示的默认 CNAME 值完成校验，看到验证通过后再改成 china 版**。
- 若 DNS 托管在 Cloudflare：**验证阶段用灰云（DNS only）**，看到两个绿勾之后再决定要不要开代理。

### 5.4 换平台并不是更好的选择

在没有自己域名的情况下，**境外平台普遍**存在同样的大陆可达性问题，换一家未必更好。已核实的两例：

- **腾讯云 EdgeOne Makers**：免费版额度很大、支持 Next.js SSR，但官方文档写明其**默认域名在中国大陆网络下只能用"系统生成的预览链接"访问、有效期 3 小时**；想稳定访问必须绑定**已备案**的域名。
- **CloudBase 默认域名**：大陆可访问，但官方定位是"仅适用于开发测试阶段"，且 2025-10-09 之后创建的环境会**长期带一个访问提示中间页**；官方建议生产环境绑定已备案的自定义域名。

→ 结论：**"有没有域名"才是决定大陆能否访问的变量，不是"用哪个平台"。**

### 5.5 赛事侧的口径（降低这一项的紧迫性）

《赛事群答疑》里老师原话：**"有链接就可以了，可以用 Vercel，其他的也可以"**，且 **"初赛提交 demo 即可，不用部署到正式环境"**。→ 链接不是初赛的硬门槛，但既然只需一个域名就能解决，建议还是把它配好。

---

## 6. 还没验 / 已知未做

| 项 | 状态 |
| --- | --- |
| **自定义域名接入（大陆访问）** | **待做 —— 当前唯一挡路项**，见 §5.3 |
| 自定义域名接入后的复测 | 待做。配好后需要**再跑一次 §4 的两项验证**，并**重新做一次手机真机实测** |
| 线上抗压 / 并发 | 未测。Vercel Hobby 并发部署数为 1，够用即可 |
| 预热与冷启动 | 未测。首次访问可能有冷启动延迟 |
| `SERVER_CODEBUDDY_API_KEY` 与 AI 层 | 未接。接入前建议单独测一次纯 API 延迟（历史实测里 CLI 调用一次约 37 秒，直连 HTTP API 未必这么慢，但需要实测） |
| C 模块的教师/FAQ demo 迁入主工程 | 未做。目前仓库里没有这部分代码与数据 |

---

## 7. 复现方式

全部是标准 HTTP 调用与仓库内脚本，不依赖任何专有 SDK。

```bash
# 线上自检（不需要密钥）
curl -s https://ai-science-research.vercel.app/api/ping

# 线上隔离验收（需要本地 .env.local，仅用于跑完后的清理）
node --env-file=.env.local scripts/verify/identity-isolation.mjs https://ai-science-research.vercel.app
```

本地对照：

```bash
node_modules/.bin/next build && node_modules/.bin/next start -p 3100
node --env-file=.env.local scripts/verify/identity-isolation.mjs http://127.0.0.1:3100
```

> 💡 **在需要走代理的机器上跑上面的 Node 脚本时，要加 `NODE_USE_ENV_PROXY=1`。**
> Node 的 `fetch` 默认**不读** `HTTP_PROXY`（只有 curl 读），否则会报 `UND_ERR_CONNECT_TIMEOUT`，很容易误判成"部署失败"。

---

## 修订记录

| 版本 | 日期 | 改动 |
| --- | --- | --- |
| **v2** | 2026-09-20 | §5 补完大陆可达性实测：确认 `*.vercel.app` 被 DNS 污染 + TLS 重置；用 Vercel 托管的自定义域名（`react.dev`、`tRPC.io`）做对照，证明 IP 层未被封、换自定义域名即可解决；补充换平台的对比与建议 |
| **v1** | 2026-09-20 | 建立。首次部署到 Vercel（`hkg1`）并完成线上验证：自检接口 `database: connected`、隔离验收 44/44、区域确认为香港。记录部署权限限制、环境变量陷阱 |
