# 业务模块

按功能新增 assessment、profile、roadmap、dashboard、resources、questions 等子目录。这里放业务交互组件、纯规则与相关测试；HTTP 页面入口放 src/app，服务端调用放 src/server。

第一条流程的字段已经定下来了：见 [`src/contracts/`](../contracts/README.md)（内含会被编译校验的最小样例）。其中画像、路线、任务三份标着「待 B 确认」，确认后即可开工。
